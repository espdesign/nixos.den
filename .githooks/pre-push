#!/usr/bin/env bash
set -euo pipefail

# final local checks before pushing to remote
echo "🚀 Running pre-push checks..."

# Run the project lint script directly (which self-bootstraps using locked inputs)
./.github/scripts/lint.sh

echo ""
echo "✅ All checks passed! Proceeding with push."
